%АВТОР% Симаков В.А., аспирант
%АФФИЛИАЦИЯ% ФГБОУ ВО «Смоленский государственный университет»
%ЗАГОЛОВОК% АЛГЕБРАИЧЕСКАЯ МАШИНА БАЗ ДАННЫХ НА ОСНОВЕ (λ, μ)-СВЁРНУТОГО ПРОИЗВЕДЕНИЯ: ТРАНСЛЯЦИЯ SQL-ЗАПРОСОВ В ЦЕПОЧКИ МНОГОМЕРНО-МАТРИЧНЫХ ОПЕРАЦИЙ

**Аннотация.** Описана программная реализация алгебраической машины баз данных, транслирующей SQL-подобный запрос в цепочку (λ, μ)-свёрнутых произведений многомерных матриц Н.П. Соколова. Установлено взаимно однозначное соответствие ролей индексов свёрнутого произведения конструкциям языка запросов. Доказана теорема о разложении многооперандного стягивания в композицию бинарных свёрнутых произведений, из которой следует независимость результата от порядка свёрток и корректность оптимизации плана. Найдено точное условие обратимости отображения отношения в гиперкуб. Показано, что агрегаты MIN, MAX и COUNT DISTINCT, невыразимые над полем действительных чисел, выражаются над другими полукольцами. Показано, что (λ, μ)-произведение при ненулевом числе скоттовых индексов сводится к пакетному матричному умножению разложением на сечения; на версии библиотеки, не выполнявшей такого сведения самостоятельно, это давало ускорение в 7.7–14.3 раза, а в последующей версии тот же путь найден самой библиотекой. Проведено сравнение с колоночной СУБД DuckDB и с ClickHouse на одиннадцати запросах с построчной сверкой результатов: преимущество составляет от 1.3 до 4.5 раза против DuckDB и от 3.6 до 66 раз против ClickHouse. Оно **растёт с объёмом фактов** — от 3.2 раза при 100 тыс. строк до 7.8 раза при 5 млн, поскольку размер гиперкуба от числа фактов не зависит, — и теряется, когда на ячейку приходится менее одного факта. Симметричное сравнение, где всем системам предоставлена предпосчитанная сводка одинаковой гранулярности, отделяет вклад алгебры от вклада предварительной агрегации.

**Ключевые слова:** многомерные матрицы, (λ, μ)-свёрнутое произведение, алгебраическая машина баз данных, OLAP, гиперкуб, einsum, трансляция запросов, полукольца.

%АВТОР_EN% Simakov V.A.
%АФФИЛИАЦИЯ_EN% Smolensk State University
%ЗАГОЛОВОК_EN% AN ALGEBRAIC DATABASE MACHINE BASED ON THE (λ, μ)-CONVOLVED PRODUCT: TRANSLATION OF SQL QUERIES INTO CHAINS OF MULTIDIMENSIONAL MATRIX OPERATIONS

**Abstract.** We present a software implementation of an algebraic database machine that translates an SQL-like query into a chain of (λ, μ)-convolved products of N.P. Sokolov's multidimensional matrices. A one-to-one correspondence is established between the index roles of the convolved product and the constructs of the query language. A theorem is proved on the decomposition of a multi-operand contraction into a composition of binary convolved products, implying that the result is independent of the contraction order and hence that plan optimization is sound. An exact condition for the invertibility of the mapping from a relation to a hypercube is found. The aggregates MIN, MAX and COUNT DISTINCT, which are not expressible over the field of real numbers, are shown to be expressible over other semirings. We further show that, when the number of Scott indices is non-zero, the (λ, μ)-product reduces to a batched matrix multiplication by decomposition into sections; on a library version that did not perform this reduction on its own this yielded a speedup of 7.7 to 14.3, while a later version found the same route by itself. A comparison against the columnar DBMS DuckDB and against ClickHouse was carried out on eleven queries with row-by-row verification of results: the speedup ranges from 1.3 to 4.5 against DuckDB and from 3.6 to 66 against ClickHouse. It **grows with the number of facts** — from 3.2 at 100 thousand rows to 7.8 at 5 million — because the size of the hypercube does not depend on the number of facts, and it is lost when fewer than one fact falls on a cell. A symmetric comparison, in which every system is given a pre-aggregated summary of the same granularity, separates the contribution of the algebra from that of pre-aggregation.

**Keywords:** multidimensional matrices, (λ, μ)-convolved product, algebraic database machine, OLAP, hypercube, einsum, query translation, semirings.

## Введение

В работе [2] показано, что (λ, μ)-свёрнутое произведение многомерных матриц, введённое Н.П. Соколовым [1], естественно выражается через соглашение о суммировании Эйнштейна и его программную реализацию — функцию einsum, а на больших тензорах такая реализация опережает наивную индексно-ориентированную на два порядка. Это результат об операции. Оставался открытым вопрос об инструменте: можно ли на этой операции построить работающую машину баз данных.

Идея строить системы управления базами данных на алгебре многомерных матриц восходит к работам В.И. Мунермана и соавторов [3–7], где многомерно-матричная модель обоснована как альтернатива теоретико-множественной и установлено соответствие между моделями. Настоящая работа продолжает это направление и добавляет к нему три результата, необходимые для перехода от модели к работающей машине. Первый — точное условие, при котором соответствие реляционной и многомерно-матричной моделей на уровне экземпляров взаимно однозначно (предложение 1). Второй — правила перевода конструкций языка запросов в операции алгебры, выписанные для полного подмножества языка, вместе с описанием класса представимых запросов (теорема 1). Третий — доказательство того, что перестановка операций при оптимизации плана не меняет результата (теорема 2); без него оптимизатор не вправе выбирать порядок свёрток. Схема распараллеливания сопоставлена с алгоритмом работы [6] в §3.4.

Построена программная реализация объёмом около шести тысяч строк, в которой реляционные таблицы фактов отображаются в гиперкубы, а справочники — в измерения с атрибутами и мерами; SQL-подобный запрос разбирается, связывается с каталогом метаданных и транслируется в план — цепочку (λ, μ)-свёрнутых произведений; план оптимизируется выбором порядка свёрток и исполняется через einsum поверх BLAS либо, при высокой разреженности, собственным разреженным ядром. Корректность проверяется 242 автоматическими тестами, из которых 76 сверяют реализацию с алгебраическими законами.

Используемые обозначения сведены в табл. 1.

%ПОДПИСЬ% Таблица 1 — Обозначения

| Обозначение | Значение |
|---|---|
| K = (K, ⊕, ⊗, 0, 1) | коммутативное полукольцо коэффициентов; 0 и 1 — его нейтральные элементы |
| A: I_{1} × … × I_{p} → K | многомерная матрица (гиперкуб) ранга p |
| L, S, C, M | группы индексов: свободные A, скоттовы (λ), кэлиевы (μ), свободные B |
| A ∗_{(λ, μ)} B | (λ, μ)-свёрнутое произведение |
| φ(R) | гиперкуб, построенный по отношению R |
| π, m | проекция кортежа на измерения и его мера |
| H | матрица перехода иерархии измерений |
| χ | индикатор условия отбора |
| θ | фактор заполнения гиперкуба |

## 1. Модель данных и отображение отношения в гиперкуб

Измерением называем конечное упорядоченное множество значений вместе с биекцией, сопоставляющей значению его порядковый номер. Эта биекция превращает произвольные значения (идентификаторы, строки, даты) в индексы матрицы. Словарь измерения расширяется только добавлением в конец: при дозагрузке новые значения получают следующие ординалы, а существующие сохраняют свои, иначе ранее построенные матрицы перестали бы соответствовать индексации.

Гиперкубом ранга p называем многомерную матрицу A: I_{1} × … × I_{p} → K, где I_{j} — множество ординалов j-го измерения, а K — коммутативное полукольцо коэффициентов (в базовом случае — поле действительных чисел).

Пусть R — мультимножество кортежей таблицы фактов, π — проекция кортежа на набор измерений, m — мера. Отображение отношения в гиперкуб определим как

%ФОРМУЛА% φ(R)[x] = ⊕_{r ∈ F(x)} m(r),   где F(x) = π^{−1}(x),

то есть ячейка есть ⊕-сумма меры по слою F(x) — прообразу точки x; в терминах теории меры это прямой образ меры m вдоль проекции π. Вычисление векторизуется: координаты кортежей сворачиваются в линейный индекс, после чего суммирование по слоям выполняется одной операцией подсчёта со взвешиванием, со сложностью O(|R|·p) без явного построения слоёв.

**Утверждение 1 (гомоморфность).** φ(R_{1} ∪ R_{2}) = φ(R_{1}) ⊕ φ(R_{2}), где ∪ понимается как объединение мультимножеств, то есть с сохранением кратностей. Действительно, слой объединения есть дизъюнктное объединение слоёв, а ⊕-сумма по дизъюнктному объединению равна сумме частичных сумм.

**Предложение 1 (условие обратимости).** Ограничение φ на отношения с ненулевыми мерами инъективно тогда и только тогда, когда π инъективно на R, то есть набор измерений является ключом отношения фактов.

*Доказательство.* Если π инъективно, каждый слой содержит не более одного кортежа, значит φ(R)[x] = m(r) для единственного r с π(r) = x, и отношение восстанавливается по гиперкубу: ненулевые ячейки взаимно однозначно соответствуют кортежам. Обратно, пусть π(r_{1}) = π(r_{2}) для различных r_{1} ≠ r_{2}. Возьмём два отношения, различающиеся лишь распределением меры между этими кортежами при одинаковой сумме: они различны, но их образы совпадают. □

Оговорка о ненулевых мерах в формулировке существенна и не является техническим удобством. Ячейка со значением 0 неотличима от ячейки, которой не соответствует ни одного кортежа, поэтому кортеж с нулевой мерой отображением теряется; на знакопеременных мерах теряется больше — два кортежа одного слоя с мерами +a и −a дают ту же ячейку, что и пустой слой, так что инъективность рушится даже при инъективном π. Практически это устраняется тем же спутниковым счётным гиперкубом (см. ниже): пара (сумма, мощность слоя) отличает пустой слой от слоя с нулевой суммой. Без него условие предложения 1 следует читать как требование строгой положительности меры.

Из предложения 1 следуют три обстоятельства.

Во-первых, соответствие реляционной и многомерно-матричной моделей на уровне экземпляров в общем случае является гомоморфизмом, но не изоморфизмом: изоморфизм имеет место в точности тогда, когда гранулярность гиперкуба совпадает с гранулярностью отношения, то есть когда набор измерений образует ключ отношения фактов. Предложение 1 и указывает, чем выделены те случаи, в которых модели действительно изоморфны.

Во-вторых, поскольку слой схлопывается ⊕-суммой, максимум по ячейкам гиперкуба не равен максимуму по кортежам, когда слой содержит более одного кортежа. Меры 3 и 5, попавшие в одну ячейку, и мера 4 в другой дают куб {8, 4}: максимум по кубу равен 8, по кортежам — 5. Это свойство отображения, а не дефект реализации, и оно должно быть зафиксировано в документации системы.

В-третьих, пара из гиперкуба сумм и гиперкуба, построенного по мере, тождественно равной единице, различает больше, чем один лишь гиперкуб сумм: она сохраняет мощность каждого слоя. Поэтому при загрузке факта создаётся спутниковый счётный гиперкуб, благодаря которому агрегаты COUNT и AVG вычисляются точно, а не по непустым ячейкам.

Иерархии измерений (дата → месяц → квартал, товар → категория) задаются отображением h из значений дочернего измерения в значения родительского и представляются матрицей перехода H, у которой H[i, j] = 1, если h(v_{i}) = v_{j}, и 0 иначе. Для цепочки иерархий матрица перехода есть матричное произведение соответствующих матриц, поскольку в сумме по промежуточному индексу ровно одно слагаемое отлично от нуля.

## 2. Трансляция запроса в цепочку свёрнутых произведений

Напомним определение [1, 2]; изложение теории многомерных матриц см. также в [8]. Для матриц A: I_{L} × I_{S} × I_{C} и B: I_{S} × I_{C} × I_{M}

%ФОРМУЛА% (A ∗_{(λ, μ)} B)[L, S, M] = ⊕_{C} A[L, S, C] ⊗ B[S, C, M],

где суммирование ведётся по всем наборам значений группы C, то есть по I_{C}; группа S мощности λ — общие индексы, сохраняемые в результате, группа C мощности μ — общие индексы, по которым идёт суммирование, а группы L и M — свободные индексы сомножителей. Следуя [6, 7], общие индексы будем называть **скоттовыми** (группа S) и **кэлиевыми** (группа C). Именно наличие скоттовых индексов отличает эту операцию от умножения обычных матриц и от тензорной свёртки: при λ = 0 она в них вырождается.

Ранг результата равен

%ФОРМУЛА% r = p + q − λ − 2μ,

то есть при различных сочетаниях λ и μ пробегает все значения от 0 (скаляр) до p + q (по существу декартово произведение). Порядок параметров в обозначении существен и здесь принят тот же, что в [7]: **первый — число скоттовых индексов, второй — число кэлиевых**.

Отсюда важное для дальнейшего обстоятельство: при μ = 0 суммирования нет вовсе, операция чисто мультипликативна, и ранг результата **растёт**. Случай не вырожденный: в настоящей работе (1, 0)-произведения возникают в двух местах — условие отбора есть умножение на индикатор без свёртки, а соединение двух таблиц фактов по общему измерению, остающемуся в группировке, повышает ранг результата над рангом каждого из сомножителей.

Транслятор построен на том, что эти роли находятся во взаимно однозначном соответствии с конструкциями языка запросов (табл. 2).

%ПОДПИСЬ% Таблица 2 — Соответствие ролей индексов конструкциям SQL

| Роль индекса | Условие | Конструкция запроса |
|---|---|---|
| λ, скоттов (общий, сохраняется) | ось общая для операндов и присутствует в GROUP BY | соединение по ключу, остающемуся в группировке |
| μ, кэлиев (общий, свёртывается) | ось общая, в GROUP BY отсутствует | соединение по ключу с последующей агрегацией |
| свободный | ось принадлежит одному операнду | измерение, уникальное для данного факта |

Соединение и агрегация суть одна операция, различающаяся лишь тем, попал общий индекс в результат или нет. Остальные конструкции языка получаются как следствия.

**Отбор.** Пусть условие φ зависит от нескольких измерений и χ — его индикатор, то есть матрица со значениями 0 и 1 на соответствующих осях. Сумма по ячейкам, удовлетворяющим φ, равна сумме по всем ячейкам произведения A ⊗ χ, поскольку не удовлетворяющие ячейки умножаются на нуль. Следовательно условие отбора — не проход по данным, а дополнительный операнд той же свёртки. Конъюнкция условий по разным измерениям остаётся факторизованной, тогда как дизъюнкция требует материализации совместного индикатора по формуле χ = 1 − (1 − χ_{1})(1 − χ_{2}); это причина, по которой OR между разными измерениями дороже, чем AND. Приведённая формула записана для булевых индикаторов над (+, ·) и опирается на равенство χ^{2} = χ; над произвольным полукольцом дизъюнкция выражается через ⊕ булева полукольца (∨, ∧), а не через вычитание, которого в полукольце может не быть.

**Свёртка по иерархии.** Группировка по родительскому измерению есть (0,1)-свёртка с матрицей перехода: сумма произведений A[…,c]·H[c,p] по c равна сумме A[…,c] по прообразу p. Отдельного кода агрегации по иерархии не требуется.

**Оконные функции.** Накопительная сумма вдоль оси есть умножение на верхнетреугольную матрицу; скользящее окно ширины w — на ленточную; взвешенное окно — на произвольную матрицу весов.

**Разграничение доступа.** Право роли на измерение задаётся вектором из нулей и единиц и добавляется в тот же терм ещё одним операндом; запрещённые данные не участвуют в арифметике ни на одном промежуточном шаге.

Пример. Запрос

%КОД% SELECT customer, month, SUM(quantity * price)
%КОД% FROM sales JOIN product ON sales.product = product.product
%КОД% WHERE region = 'Смоленск'
%КОД% GROUP BY customer, month

транслируется в единственное выражение

%КОД% np.einsum('abc,b,cd,a->ad', sales, price, date_to_month, region_mask),

где sales — гиперкуб продаж с осями (клиент, товар, дата), price — куб ранга 1 с ценами, date_to_month — матрица перехода иерархии, region_mask — индикатор условия отбора. Индексы b (товар) и c (дата) общие и отсутствуют в результате — это μ-индексы; индекс a (клиент) общий с маской и присутствует в результате — λ-индекс.

**Теорема 1 (о представимости).** Всякий запрос, в котором агрегатами служат SUM, COUNT и AVG, выражения под агрегатами построены из мер, констант, операций сложения, вычитания, умножения и деления на ненулевую константу, условие отбора есть произвольная булева комбинация предикатов над измерениями и их атрибутами, а каждое измерение группировки является осью куба либо достижимо по цепочке иерархий, представим как линейная комбинация (λ, μ)-свёрнутых произведений.

*Доказательство* конструктивно и повторяет работу транслятора: выражение под агрегатом приводится к сумме произведений мер с коэффициентами индукцией по структуре с использованием дистрибутивности умножения над сложением; каждое слагаемое даёт отдельное стягивание, к операндам которого добавляются индикатор условия и матрицы перехода иерархий. □

**Утверждение 2 (о непредставимости MIN и MAX).** Агрегаты MIN и MAX не выражаются никакой линейной комбинацией (λ, μ)-свёрнутых произведений над полем действительных чисел с фиксированными вспомогательными матрицами.

*Доказательство.* Свёрнутое произведение билинейно, поэтому любая такая комбинация есть линейная, а значит аддитивная функция гиперкуба-меры. Но max((1,0)) + max((0,1)) = 2, тогда как max((1,0) + (0,1)) = max((1,1)) = 1. □

Исключение MIN и MAX из модели есть свойство алгебры, а не недоработка реализации; в §5 показано, что они возвращаются в модель при замене полукольца коэффициентов.

## 3. План запроса как терм алгебры и модель стоимости

### 3.1. Разложение плана

Выражение с четырьмя операндами не является операцией алгебры Соколова: последняя бинарна. Пусть матрицы A_{1}, …, A_{n} индексированы наборами имён X_{1}, …, X_{n}, положим X = X_{1} ∪ … ∪ X_{n}, и пусть O ⊆ X — набор выходных индексов. Многооперандным стягиванием назовём

%ФОРМУЛА% T[O] = ⊕_{ν} A_{1}[ν_{1}] ⊗ … ⊗ A_{n}[ν_{n}],

где ν пробегает все наборы значений невыходных индексов, то есть I_{X \ O}, а ν_{i} — сужение на имена X_{i} полного набора значений, составленного из ν и значений выходных индексов. Планом назовём двоичное дерево с листьями A_{1}, …, A_{n}; вычисление идёт снизу вверх бинарными (λ, μ)-произведениями, где μ-группу образуют общие индексы, не нужные ни далее, ни в выходе, а λ-группу — прочие общие индексы.

**Теорема 2 (о разложении).** Для любого плана результат его вычисления равен многооперандному стягиванию.

*Доказательство.* Индукция по числу операндов. База очевидна. Пусть корень объединяет поддеревья с наборами операндов P и Q. Разобьём невыходные индексы на встречающиеся только в P, только в Q и общие. Так как индексы первого класса не входят в множители из Q и наоборот, соответствующие суммы выносятся за скобку по правой дистрибутивности умножения над конечной суммой, и стягивание распадается на внешнюю сумму по общим невыходным индексам от произведения двух внутренних выражений. Внутренние выражения по предположению индукции равны результатам поддеревьев, а внешняя сумма есть в точности (λ, μ)-произведение. □

**Следствие 1.** Результат не зависит от порядка свёрток: любые два плана дают одно и то же.

Следствие 1 делает оптимизацию корректной — выбирая порядок по стоимости, оптимизатор не меняет ответ. Оно же обосновывает приём, применяемый при превышении технического ограничения einsum на число различных индексов (52 буквы латинского алфавита в обоих регистрах): план разбивается на цепочку парных произведений с промежуточной материализацией.

Для приведённого выше запроса разложение имеет вид, показанный в табл. 3; численная проверка показывает, что последовательное применение операций ядра даёт тот же результат, что и единый вызов einsum, с относительным расхождением менее 10^{−12}.

%ПОДПИСЬ% Таблица 3 — Разложение плана запроса в композицию бинарных произведений

| Шаг | Операнды | Результат | Тип свёртки |
|---|---|---|---|
| 1 | [клиент, товар, дата] ∗ [товар] | [клиент, дата] | (0,1), μ: товар |
| 2 | [дата, месяц] ∗ [клиент, дата] | [месяц, клиент] | (0,1), μ: дата |
| 3 | [клиент] ∗ [месяц, клиент] | [клиент, месяц] | (1,0), λ: клиент |

### 3.2. Функция стоимости

Пусть |I_{X}| обозначает произведение мощностей осей группы X. Для одного бинарного произведения A: I_{L} × I_{S} × I_{C} и B: I_{S} × I_{C} × I_{M}

%ФОРМУЛА% число ⊗-операций = |I_{L}| · |I_{S}| · |I_{C}| · |I_{M}|,
%ФОРМУЛА% число ⊕-операций = (|I_{C}| − 1) · |I_{L}| · |I_{S}| · |I_{M}|,
%ФОРМУЛА% размер результата = |I_{L}| · |I_{S}| · |I_{M}| ячеек.

Стоимость плана есть сумма стоимостей его узлов, а требование к памяти — максимум размеров промежуточных результатов. Оптимизатор вычисляет обе величины до аллокации и отклоняет план, чей наибольший промежуточный результат превышает заданный бюджет, с объяснением причины, вместо того чтобы аварийно завершиться во время счёта.

Для разреженных операндов формула заменяется на оценку, учитывающую фактор заполнения θ: для случая «разреженная × плотная» число ⊗-операций равно nnz · |I_{M}|, для случая «разреженная × разреженная» — сумме по общим ключам произведений мощностей соответствующих подсписков, что в худшем случае даёт произведение чисел ненулевых элементов.

### 3.3. Выбор порядка свёрток

Задача поиска плана минимальной стоимости эквивалентна задаче оптимального стягивания тензорной сети и в общем случае NP-трудна: число различных деревьев растёт как двойной факториал (2n−3)!! — пятнадцать при n = 4 и свыше 3.4·10^{7} при n = 10.

При n ≤ 8 применяется полный перебор динамическим программированием по подмножествам операндов: для каждого подмножества запоминается минимальная стоимость его стягивания и соответствующий набор оставшихся индексов, после чего оптимум для объединения подмножеств получается перебором всех разбиений на две части.

При n > 8 применяется жадная стратегия: на каждом шаге среди всех пар оставшихся операндов выбирается пара с наименьшим значением функции стоимости данного шага; выбранная пара заменяется результатом, и шаг повторяется до единственного операнда. Жадный выбор не гарантирует оптимума, но по следствию 1 любой его результат корректен, а стоимость на планах рассматриваемого класса (три–пять операндов) от оптимальной практически не отличается.

Найденный порядок кэшируется вместе с планом: как показано в §4.6, его пересчёт при каждом вызове на малых операндах стоит дороже самой свёртки.

Эффект от выбора порядка иллюстрирует рассматриваемый запрос. При мощностях измерений 100, 100, 100 и 4 стягивание по полному индексному пространству потребовало бы 4·10^{7} операций, тогда как оптимальный порядок даёт 10^{6} + 4·10^{4} + 4·10^{2}, то есть около 1.04·10^{6} — выигрыш почти в сорок раз при неизменном результате. Наибольший промежуточный результат при этом составляет 10^{4} ячеек.

### 3.4. Сопоставление схемы распараллеливания

Разбиение работы между узлами естественно вести по оси, присутствующей в результате: подзадачи выходят независимыми, и межузловая редукция не нужна. Но оси результата неравноценны по объёму пересылок.

В [6] доказано утверждение: при λ > 0 результат (λ, μ)-свёрнутого произведения составлен из сечений ориентации скоттовых индексов, каждое из которых есть произведение соответствующих сечений сомножителей с тем же набором значений этих индексов. Иначе говоря, одно (λ, μ)-произведение распадается на n^λ независимых (0, μ)-произведений над сечениями.

Отсюда следует, что разбивать выгоднее именно по скоттовой оси: она режет **оба** сомножителя, и узел получает лишь свои срезы каждого. Разбиение по свободной оси режет только один операнд, а второй приходится реплицировать на все узлы целиком. Реализация следует этому порядку предпочтения; правило подтверждено измерением, приведённым в §4.9.

Различие сохраняется на внутреннем уровне. В [6] полученные (0, μ)-произведения вычисляются обобщением алгоритма Кэннона — с систолической циркуляцией блоков между процессорами распределённой памяти и пересчётом блочных индексов через смешанные системы счисления; при этом требуется, чтобы мощности всех измерений совпадали. Настоящая работа на внутреннем уровне полагается на многопоточную реализацию BLAS в пределах одного узла, распределённого умножения не выполняет, но и ограничения на равенство мощностей измерений не накладывает.

## 4. Экспериментальная оценка

Испытания проводились на процессоре AMD Ryzen 7 PRO 3700 (8 ядер, 16 потоков) под Ubuntu 24.04, Python 3.12.3, NumPy 2.5.2 с библиотекой scipy-openblas 0.3.34, pandas 3.0.5, DuckDB 1.5.5, ClickHouse 26.7.2 во встраиваемой сборке chdb 4.3.0. Использован тот же процессор, что и в работе [2], что делает сверку с опубликованными там результатами методически корректной. Каждый замер — минимум из нескольких прогонов после прогрева.

Версии библиотек указаны не для полноты. Производительность einsum на свёртках со скоттовыми индексами между выпусками NumPy изменилась в двадцать раз (§4.1), поэтому среда замера здесь — часть самого замера. В репозитории эти версии закреплены отдельным набором зависимостей, чтобы приведённые числа воспроизводились.

Сравнение с колоночной СУБД устроено следующим образом. **Результаты сверяются построчно**, и расхождение считается провалом запроса; набор из одиннадцати запросов включает заведомо невыгодный для матричного подхода случай — запрос, не сворачивающий ни одного измерения; помимо замера в одной точке проведены развёртки по объёму фактов, размеру гиперкуба и перекосу распределения, поскольку именно развёртки, а не единичный замер, позволяют указать границу применимости.

В качестве системы сравнения взята DuckDB — колоночная аналитическая СУБД, работающая **в одном процессе и в общей памяти** с измеряемой реализацией, что исключает из замера сетевой обмен, сериализацию и клиент-серверные накладные расходы. По устройству исполнителя — колоночное хранение, векторизованная обработка, соединение хешированием — она принадлежит тому же классу, что ClickHouse и MonetDB, поэтому измеряется разница моделей исполнения, а не транспортных уровней; распределённое поведение при таком выборе, разумеется, не охвачено. Наконец, гиперкуб строится однократно, тогда как колоночная СУБД работает по сырым таблицам и выполняет соединение при каждом запросе, поэтому сравнение отвечает на вопрос «что выгоднее при повторных обращениях к неизменному набору данных» и всюду сопровождается расчётом окупаемости построения.

### 4.1. Сверка с опубликованным замером

Воспроизведение времён (1,1)-свёрнутого произведения из [2] приведено в табл. 4. Замер поставлен как контролируемый опыт: на одной машине и в одной операционной системе меняются только версия NumPy и способ вычисления. Это оказалось необходимым — воспроизвести опубликованные числа на текущей версии библиотеки нельзя, и причина существенна сама по себе.

%ПОДПИСЬ% Таблица 4 — Время выполнения (1,1)-свёрнутого произведения, нс/операция

| Размер данных | NumPy 2.1.1, einsum | NumPy 2.1.1, gemm | NumPy 2.5.2, einsum | NumPy 2.5.2, gemm | NumPy по [2] | Go последовательный по [2] |
|---|---|---|---|---|---|---|
| 2D 10×10 | 4 040 | не применяется | 3 920 | не применяется | 4 465 | 1 686 |
| 2D 100×100 | 53 890 | не применяется | 40 810 | не применяется | 18 323 | 139 068 |
| 3D 10×10×10 | 47 150 | не применяется | 18 800 | не применяется | 20 646 | 553 848 |
| 3D 100×100×100 | 59 865 246 | **3 009 802** | **3 217 925** | 3 119 845 | 65 128 484 | 5 357 979 300 |

Читается таблица так. На NumPy 2.1.1 — версии, современной работе [2], — время по пути einsum составляет 59.9 мс против опубликованных 65.1 мс, то есть совпадает в пределах 8 %: **опубликованный результат воспроизводится, и постановка замера верна**. На той же версии разложение на сечения даёт 3.01 мс — в двадцать раз быстрее. А на NumPy 2.5.2 те же 3.2 мс даёт уже сам einsum, и разница между путями исчезает.

Иными словами, между выпусками 2.1.1 и 2.5.2 в NumPy исправлена диспетчеризация пакетных свёрток: библиотека научилась отправлять в BLAS операцию, у которой есть индекс, общий обоим сомножителям и сохраняемый в результате. Разложение на сечения, выведенное здесь из строения (λ, μ)-произведения (§4.6), опередило это исправление на одну версию и совпало с ним по величине выигрыша. Отрыв от последовательной реализации на Go при этом вырос с 88.6 раза до 1717.

Отсюда два следствия для чтения всей экспериментальной части. Во-первых, абсолютные времена операций такого рода привязаны к версии библиотеки не менее жёстко, чем к оборудованию, и указывать её обязательно. Во-вторых, пакетный gemm сохранён в реализации и после исправления в NumPy: он не проигрывает ни на одной проверенной форме (§4.6) и делает производительность независимой от эвристик диспетчеризации, которые, как показано, меняются между выпусками. Расхождение на двумерных данных разобрано в §4.6.

### 4.2. Сравнение с колоночной СУБД: по сырым фактам и по материализованному агрегату

Синтетическая звёздная схема: гиперкуб 100×100×100 (10^{6} ячеек), справочник товаров с ценами, справочник клиентов с регионами, иерархии дата → месяц → квартал. В качестве базы сравнения взята колоночная аналитическая СУБД DuckDB [11], работающая в оперативной памяти по тем же данным. Выполнялся набор из одиннадцати запросов; **результаты сверялись построчно**, поскольку замер без проверки правильности лишён смысла. Все одиннадцать результатов совпали. Выдача в обеих системах запрашивалась в виде кадра данных (см. §4.7 о влиянии формата выдачи), и обе работали на собственных внутренних представлениях: гиперкуб у одной, нативные таблицы у другой. Последнее существенно — чтение зарегистрированного кадра данных заставляет колоночную СУБД преобразовывать его при каждом запросе, и сравнение перестаёт быть сравнением исполнителей. Результаты при 500 тыс. строк фактов приведены в табл. 5.

%ПОДПИСЬ% Таблица 5 — Время выполнения запросов, мс (500 тыс. строк фактов)

| Запрос | Настоящая работа | DuckDB | Отношение | Окупаемость загрузки |
|---|---|---|---|---|
| Скалярная сумма | 0.31 | 0.86 | 2.8× | 291 запрос |
| Группировка по одному измерению | 1.02 | 1.37 | 1.3× | 458 запросов |
| Соединение со справочником | 1.21 | 2.63 | 2.2× | 112 запросов |
| Соединение и свёртка по иерархии | 3.09 | 8.71 | 2.8× | 29 запросов |
| То же с избирательным фильтром | 3.20 | 6.75 | 2.1× | 45 запросов |
| Диапазонный фильтр | 1.52 | 2.93 | 1.9× | 114 запросов |
| Средний чек и число сделок | 0.91 | 2.63 | 2.9× | 93 запроса |
| Среднее | 1.75 | 2.66 | 1.5× | 177 запросов |
| Умеренно широкий результат | 1.58 | 7.14 | 4.5× | 29 запросов |
| Широкий результат, 394 тыс. строк | 18.31 | 35.56 | 1.9× | 10 запросов |
| COUNT DISTINCT по измерению | 1.96 | 7.56 | 3.9× | 29 запросов |

Последний столбец отвечает на главный практический вопрос. Гиперкуб строится однократно за 159 мс, тогда как DuckDB работает по сырым таблицам и выполняет соединение при каждом запросе. Отсюда подход оправдан начиная с некоторого числа обращений к одному и тому же набору данных: от 10 запросов для широкой выдачи и 29 для соединения со свёрткой по иерархии до 458 для группировки по одному измерению, где абсолютная экономия мала.

Отметим наблюдение, определяющее этот порог. Основная часть времени загрузки приходится не на арифметику построения матрицы, а на кодирование значений измерений в ординалы: при пословном обходе на него уходит свыше 90 % времени, и лишь векторизация этого шага двоичным поиском по отсортированному словарю доводит загрузку до приведённых 159 мс. Для систем этого класса узкое место загрузки — перевод значений в индексы, а не свёртка. Расчёт окупаемости предполагает **однородный поток запросов** — повторение запросов того же типа по неизменным данным; при разнородной нагрузке окупаемость следует считать по средневзвешенной экономии, а при непрерывно меняющихся данных построение куба возобновляется и приведённые числа неприменимы.

Закономерность видна отчётливо, и она не в схлопывании как таковом. Преимущество на всех одиннадцати запросах умеренное — от 1.3 до 4.5 раза, — и наименьшее оно там, где запрос сводится к одной агрегации: группировка по одному измерению даёт лишь 1.3 раза, поскольку свёртка обходит миллион ячеек, а колоночная СУБД хеширует полмиллиона строк в сотню групп, и такая хеш-таблица целиком помещается в кэш процессора. Существенно, что при этом отношение времён на разных запросах различается втрое, и различие объясняется не размером результата, а числом операций, сливающихся в одно стягивание. Свёртка дешевеет с каждым свёрнутым измерением, а сборка результата — нет.

**Симметричное сравнение.** Приведённый замер сопоставляет предпосчитанную сводку с обходом сырых фактов, то есть сравнивает не столько исполнители, сколько модели хранения. Чтобы отделить вклад алгебры от вклада предварительной агрегации, поставлен второй, симметричный опыт: DuckDB получает таблицу той же гранулярности, что и гиперкуб, — факт, свёрнутый до (клиент, товар, дата) и снабжённый счётчиком строк, то есть точный аналог гиперкуба вместе со спутниковым счётным кубом. Все одиннадцать запросов переписаны под эту таблицу, результаты по-прежнему сверяются построчно. Замер при 2 млн строк фактов приведён в табл. 6.

%ПОДПИСЬ% Таблица 6 — Симметричное сравнение: обе системы стартуют из предпосчитанной сводки, мс (2 млн строк фактов)

| Запрос | Настоящая работа | DuckDB, сырые факты | DuckDB, агрегат | Отношение к агрегату |
|---|---|---|---|---|
| Скалярная сумма | 0.40 | 1.24 | 0.96 | 2.43× |
| Группировка по одному измерению | 1.00 | 2.19 | 1.40 | 1.41× |
| Соединение со справочником | 1.20 | 3.71 | 3.03 | 2.53× |
| Соединение и свёртка по иерархии | 3.13 | 12.50 | 10.30 | 3.29× |
| То же с избирательным фильтром | 3.69 | 10.01 | 6.16 | 1.67× |
| Диапазонный фильтр | 1.34 | 3.71 | 2.91 | 2.17× |
| Средний чек и число сделок | 0.89 | 3.32 | 2.94 | 3.30× |
| Среднее | 1.71 | 3.56 | 3.11 | 1.82× |
| Умеренно широкий результат | 1.57 | 10.22 | 7.65 | 4.88× |
| Широкий результат, без свёртки | 37.28 | 197.89 | 73.30 | 1.97× |
| COUNT DISTINCT по измерению | 1.99 | 11.01 | 8.19 | 4.11× |

Опыт разделяет два источника преимущества. Предпосчёт объясняет заметную его часть: против сырых фактов отношение доходит до 5.3 раза, против агрегата той же гранулярности — до 4.9, и на простой группировке падает с 2.2 до 1.4. Но преимущество не исчезает нигде, и наибольшим оно остаётся там, где несколько операций сливаются в одно стягивание. Это и есть вклад собственно алгебры, отделённый от вклада предварительной агрегации.

Симметрия соблюдена и по стоимости построения: DuckDB строит свой агрегат за 116 мс, настоящая работа — гиперкуб за 722 мс, то есть вшестеро дольше. Гиперкуб при этом занимает 15 МиБ против 865 тыс. строк агрегата, а словари измерений добавляют к нему менее 2 % объёма (0.57 МиБ на словари при 25.6 МиБ кубов в замере с текстовыми ключами мощностью 5 тыс. и 336); каталог метаданных на этом фоне пренебрежимо мал. Вывод формулируется так: **предпосчёт объясняет преимущество на простых агрегациях, алгебра — на составных запросах.**

**Сравнение с ClickHouse.** DuckDB отвечает на возражение «сравните с колоночной СУБД». Отдельного ответа требует возражение «возьмите признанный OLAP-движок», и для него взята ClickHouse [12] во встраиваемой сборке chdb: она работает в том же процессе, что и остальные измеряемые системы, и потому сохраняет условие, на котором в стенд взята DuckDB. Выровнены четыре обстоятельства, каждое из которых иначе исказило бы результат: данные лежат в собственных таблицах каждой системы; число потоков у обоих движков равно числу ядер; столбцы объявлены не-nullable, иначе ClickHouse не принимает их в ключ сортировки и считает медленнее; наконец, у встраиваемой сборки измерены и вычтены постоянные накладные расходы на запрос — 0.70 мс, что на запросах в единицы миллисекунд составляет заметную долю. Результаты сверялись построчно во всех трёх системах и совпали. Замер при 5 млн строк фактов приведён в табл. 7.

%ПОДПИСЬ% Таблица 7 — Три исполнителя на одном наборе, мс (5 млн строк фактов)

| Запрос | Настоящая работа | DuckDB | ClickHouse | Отношение к DuckDB | Отношение к ClickHouse |
|---|---|---|---|---|---|
| Скалярная сумма | 0.34 | 2.45 | 2.47 | 7.2× | 7.2× |
| Группировка по одному измерению | 1.03 | 3.91 | 4.46 | 3.8× | 4.3× |
| Соединение со справочником | 1.14 | 6.74 | 19.40 | 5.9× | 17.0× |
| Соединение и свёртка по иерархии | 7.68 | 24.81 | 57.62 | 3.2× | 7.5× |
| То же с избирательным фильтром | 3.09 | 14.17 | 203.75 | 4.6× | 66.0× |
| Диапазонный фильтр | 1.47 | 5.71 | 19.16 | 3.9× | 13.1× |
| Средний чек и число сделок | 1.16 | 5.83 | 20.84 | 5.0× | 18.0× |
| Умеренно широкий результат | 1.63 | 23.46 | 60.58 | 14.4× | 37.1× |
| Широкий результат, без свёртки | 43.13 | 266.83 | 199.03 | 6.2× | 4.6× |
| COUNT DISTINCT по измерению | 2.09 | 21.75 | 7.60 | 10.4× | 3.6× |

Результат для ClickHouse оказался неожиданным и заслуживает оговорки: на звёздной схеме она уступает DuckDB почти везде, и тем сильнее, чем больше в запросе соединений — от 2.9 раза на соединении со справочником до четырнадцати на соединении с фильтром. Это согласуется с её устройством: ClickHouse рассчитана на широкие плоские таблицы и в собственной документации рекомендует денормализацию, тогда как рассматриваемый набор запросов нормализован по определению. Обратное верно там, где соединений нет: точный подсчёт различных значений выполняется втрое быстрее, чем в DuckDB, и на широкой выдаче ClickHouse также впереди.

Для настоящей работы существенно не то, какая из двух колоночных систем быстрее, а то, что добавление второй не изменило картины: преимущество матричного исполнения сохраняется против обеих и в том же диапазоне.

### 4.3. Зависимость от объёма фактов

Замер в одной точке говорит мало, поэтому проведены развёртки по трём параметрам. Первая — по числу фактов при неизменном гиперкубе (табл. 8).

%ПОДПИСЬ% Таблица 8 — Преимущество при росте числа фактов, гиперкуб 100×100×100

| Строк фактов | Заполненность | Загрузка, мс | Свёртка двух осей | Соединение и иерархия | Широкая выдача |
|---|---|---|---|---|---|
| 100 000 | 9.5 % | 37 | 1.16× | 3.19× | 2.41× |
| 500 000 | 39.4 % | 155 | 1.24× | 3.77× | 2.07× |
| 2 000 000 | 86.5 % | 787 | 2.10× | 4.26× | 5.64× |
| 5 000 000 | 99.3 % | 1 845 | 3.61× | 7.80× | 5.80× |

Результат существен и объясняется устройством модели: **гиперкуб есть сводка постоянного размера**. Сколько бы фактов ни поступило, число ячеек остаётся равным произведению мощностей измерений, и время свёртки не меняется — растёт лишь заполненность. Колоночная же СУБД обрабатывает при каждом запросе всё большее число строк. Поэтому преимущество не постоянно, а **растёт линейно с объёмом фактов**: от 3.2 раза при 100 тыс. строк до 7.8 раза при 5 млн на запросе с соединением и свёрткой по иерархии, и от 1.2 до 3.6 раза на простой свёртке. Рост наблюдается по всем трём столбцам, то есть определяется не видом запроса, а самим устройством модели.

Обратная сторона видна в столбце загрузки: однократное построение куба растёт линейно и при 5 млн строк занимает 1.8 с. Это плата за постоянство размера сводки — впрочем, после устранения узкого места в кодировании измерений (§4.2) плата уже невелика.

### 4.4. Зависимость от размера гиперкуба и граница применимости

Вторая развёртка — по размеру гиперкуба при неизменных двух миллионах фактов (табл. 9). Число ячеек растёт кубически с мощностью измерений.

%ПОДПИСЬ% Таблица 9 — Преимущество при росте гиперкуба, 2 млн фактов

| Гиперкуб | Ячеек | Заполненность | Память, МиБ | Свёртка двух осей | Соединение и иерархия | Широкая выдача |
|---|---|---|---|---|---|---|
| 50×50×50 | 125 тыс. | 100 % | 2 | 8.09× | 21.49× | 7.21× |
| 100×100×100 | 1 млн | 86.5 % | 15 | 1.88× | 3.61× | 5.31× |
| 150×150×150 | 3.4 млн | 44.7 % | 51 | 0.69× | 1.46× | 3.90× |
| 200×200×200 | 8 млн | 22.1 % | 122 | **0.36×** | **0.64×** | 3.71× |

Здесь и проходит граница применимости. На кубе 200×200×200 матричное исполнение **проигрывает** колоночной СУБД и на простой агрегации (0.36×), и на соединении со свёрткой по иерархии (0.64×): свёртка обходит все восемь миллионов ячеек, из которых заполнена лишь пятая часть, тогда как DuckDB обрабатывает ровно два миллиона существующих строк. Преимущество сохраняется лишь на широкой выдаче, где обе системы заняты сборкой результата, а не счётом.

Третья развёртка — по перекосу распределения при неизменных числе фактов и размере куба (табл. 10). Степенной закон означает, что немногие клиенты и товары дают большую часть продаж, — распределение, характерное для реальной торговли.

%ПОДПИСЬ% Таблица 10 — Влияние перекоса распределения, гиперкуб 200×200×100, 2 млн фактов

| Показатель степени | Заполненность | Свёртка двух осей | Соединение и иерархия | Широкая выдача |
|---|---|---|---|---|
| 0.0 (равномерно) | 39.4 % | 0.62× | 1.18× | 4.89× |
| 0.6 | 30.7 % | 0.65× | 0.91× | 4.05× |
| 1.0 | 17.1 % | 0.66× | 1.20× | 3.66× |
| 1.4 | 7.2 % | 0.75× | 1.36× | 1.14× |

Результат на первый взгляд неожиданный: заполненность падает впятеро, а отношение времён не меняется вовсе. Объяснение простое и оно связывает все три развёртки в одно правило.

**Правило.** Время свёртки определяется **числом ячеек** гиперкуба и не зависит ни от числа фактов, ни от их распределения. Время колоночной СУБД определяется **числом строк**. Следовательно отношение времён есть функция единственной величины — числа фактов, приходящихся на ячейку.

Правило проверяется по всем трём развёрткам. В первой ячейки постоянны, факты растут — отношение растёт. Во второй факты постоянны, ячейки растут — отношение падает. В третьей постоянны и то, и другое, меняется лишь распределение — отношение неизменно. Численно совпадают и точки из разных развёрток: 3.4 млн ячеек при 2 млн фактов (0.59 факта на ячейку) дают 0.69×, а 4 млн ячеек при тех же 2 млн фактов (0.5 факта на ячейку) — 0.62×.

Отсюда практический критерий применимости: **подход выгоден, когда на ячейку гиперкуба приходится порядка одного факта и более**. При 0.25 факта на ячейку матричное исполнение уступает колоночной СУБД втрое на простой агрегации и в полтора раза на составном запросе; при 0.5 — уступает ещё; при двух фактах на ячейку выигрывает вдвое, при пяти — вчетверо. Поскольку число ячеек есть произведение мощностей измерений, критерий переводится в требование к схеме: измерения должны быть достаточно грубыми, а при мелких — гиперкуб следует строить не по всем сразу.

Заметим, что перекос распределения, вопреки ожиданию, ничего не меняет: разреженность сама по себе не помогает, пока представление остаётся плотным. Что даёт переход к разреженному представлению, показано ниже.

### 4.5. Плотное и разреженное представления

Реальные таблицы фактов дают гиперкубы с малой заполненностью, поэтому реализовано разреженное представление в координатном формате с собственными ядрами свёртки: для случая «разреженная × плотная» — выборка значений по координатам, для случая «разреженная × разреженная» — хеш-соединение по общим индексам.

Прежде замера проверено, что следствие 1 сохраняется и на разреженном пути. Теорема 2 доказана для алгебраических тождеств и от представления не зависит, однако разреженные ядра суммируют вклады в ином порядке, чем плотные, и на числах с плавающей точкой равенство могло бы нарушиться неассоциативностью округления. Цепочка из трёх разреженных гиперкубов, свёрнутая в обоих порядках, дала совпадение с максимальным расхождением 2.2·10^{−16}, то есть на уровне машинного эпсилона; оба порядка совпали и с плотным эталоном. Переписывание плана, таким образом, остаётся корректным независимо от выбранного представления, и оптимизатору не требуется знать о нём.

Замер проведён на двух запросах разной формы, и различие между ними оказалось решающим (табл. 11). Запрос Q1 — группировка по одному измерению, то есть свёртка двух осей одного куба; запрос Q2 — соединение со справочником цен и свёртка по иерархии, то есть стягивание четырёх операндов.

%ПОДПИСЬ% Таблица 11 — Плотное и разреженное представления, гиперкуб 10^{6} ячеек

| Заполненность | Память плотно, МиБ | Память разреженно, МиБ | Q1 плотно, мс | Q1 разреженно, мс | Q2 плотно, мс | Q2 разреженно, мс |
|---|---|---|---|---|---|---|
| 39.3 % | 7.63 | 12.00 | 0.77 | 8.08 | 1.04 | 29.90 |
| 13.9 % | 7.63 | 4.25 | 0.72 | 2.40 | 0.89 | 12.57 |
| 6.1 % | 7.63 | 1.85 | 0.72 | 1.05 | 0.88 | 5.94 |
| 3.1 % | 7.63 | 0.94 | 0.69 | **0.65** | 0.88 | 3.42 |
| 1.2 % | 7.63 | 0.38 | 0.66 | **0.34** | 0.93 | 1.86 |
| 0.6 % | 7.63 | 0.19 | 0.73 | **0.25** | 0.88 | 1.17 |

По памяти картина простая: разреженное представление выигрывает начиная примерно с 25 % заполнения, а при 0.6 % экономит уже в сорок раз. Время же зависит от формы запроса, и это главный результат замера.

На свёртке двух осей (Q1) разреженный путь обгоняет плотный ниже примерно 3 % заполнения и при 0.6 % оказывается быстрее втрое. Причина в том, что плотная свёртка обходит все 10^{6} ячеек независимо от того, сколько из них заполнено, тогда как разреженная работает с числом ненулевых элементов. Отметим и обратную сторону: время плотного пути в столбце Q1 не меняется вовсе — 0.66–0.77 мс на всех уровнях заполнения. Это то же правило, что и в §4.4, лишь измеренное с другой стороны.

На запросе с соединением и свёрткой по иерархии (Q2) разреженный путь не выигрывает **нигде**: при 0.6 % заполнения он на треть медленнее, а при 39 % — в двадцать девять раз. Причина в числе операндов: плотный путь стягивает все четыре одним вызовом einsum поверх BLAS, тогда как разреженный вынужден выполнять последовательность хеш-соединений, и выигрыш от малого числа ненулевых элементов съедается их стоимостью.

Отсюда правило, реализованное в системе, и его обоснование. Порог перехода к разреженному хранению установлен на уровне 2 % заполнения, то есть вблизи точки безубыточности простого запроса и заведомо ниже точки безубыточности составного. Осторожность здесь не произвол: порог един для всех запросов, а выбирать его надлежит по худшей их форме, а не по лучшей, — иначе система, ускорив Q1, замедлила бы Q2 в разы. Отсюда и направление дальнейшей работы: выбор представления следует принимать не только по заполненности, но и по числу операндов плана, то есть делать его решением оптимизатора, а не загрузчика.

### 4.6. Выбор способа обращения к BLAS

Способ обращения к BLAS определяет исход, и выбор его различен для двух случаев — при λ = 0 и при λ ≥ 1. Разберём их по порядку.

Случай λ = 0 разобран в табл. 12: для рассматриваемого запроса измерены единый вызов einsum с заранее вычисленным порядком свёрток; цепочка из явных вызовов tensordot и матричного умножения; цепочка из двух вызовов einsum.

%ПОДПИСЬ% Таблица 12 — Способы вычисления одного и того же выражения, мс

| Способ | Время |
|---|---|
| Две последовательные свёртки einsum | 0.45 |
| Единый einsum с кэшированным порядком свёрток | 1.53 |
| Цепочка tensordot и матричного умножения | 2.32 |

Явные вызовы BLAS-функций оказались самыми медленными: tensordot вынуждена копировать массив, чтобы переставить свёртываемую ось в конец, тогда как einsum стягивает без копирования. Но и единый einsum уступает разбиению того же выражения на две последовательные свёртки втрое — при том, что порядок стягивания в обоих случаях один и тот же и найден заранее. Разница здесь целиком внутри библиотеки: при четырёх операндах её планировщик выбирает иной способ исполнения, чем при двух. Прямые вызовы BLAS выигрывают лишь для ранга 2, где перестановка осей не нужна (56 мкс против 65 мкс на матрицах 100×100); постоянные накладные расходы einsum на разбор индексной спецификации составляют около 14 мкс и на малых данных доминируют — этим объясняется расхождение с [2] в двумерных строках табл. 4. В реализации введён порог, ниже которого поиск порядка свёрток не выполняется вовсе, а найденные порядки кэшируются по формам операндов.

Случай λ ≥ 1 устроен иначе, и разница существенна. Здесь у операции есть индекс, общий обоим сомножителям и **сохраняемый** в результате, а у tensordot пакетного измерения нет — и einsum_path, не найдя сведения к матричному умножению, уходит в собственный цикл на C, теряя BLAS целиком. Между тем именно λ-индексы отличают свёрнутое произведение от тензорной свёртки, то есть потеря происходит ровно в той части алгебры, которая для настоящей работы существенна.

Обход подсказан самой алгеброй. По утверждению из [6], процитированному в §3.4, при λ ≥ 1 результат составлен из сечений ориентации скоттовых индексов, каждое из которых есть (0, μ)-произведение соответствующих сечений сомножителей. Набор одинаковых (0, μ)-произведений над сечениями — это в точности то, что исполняет пакетный gemm: после перестановки скоттовых осей в начало и свёртки групп осей в по одному измерению операция записывается как

%ФОРМУЛА% A[L | S | C] → (|S|, |L|, |C|),  B[S | C | M] → (|S|, |C|, |M|),  matmul → (|S|, |L|, |M|) → C[L | S | M].

Замер приведён в табл. 13; свёртывается матрица 200×200×120 с матрицей 200×120×|M| при λ = 1, μ = 1. Как и в §4.1, замер поставлен на двух версиях NumPy, поскольку величина эффекта от версии зависит решающим образом.

%ПОДПИСЬ% Таблица 13 — Единый einsum против пакетного gemm при λ = 1, мс

| Мощность свободной оси B | NumPy 2.1.1, einsum | NumPy 2.1.1, gemm | Отношение | NumPy 2.5.2, einsum | NumPy 2.5.2, gemm | Отношение |
|---|---|---|---|---|---|---|
| 2 | 25.17 | 2.90 | **8.7×** | 3.48 | 2.81 | 1.24× |
| 8 | 40.85 | 3.67 | **11.1×** | 3.83 | 3.63 | 1.06× |
| 32 | 96.12 | 6.71 | **14.3×** | 5.62 | 5.21 | 1.08× |
| 200 | 614.62 | 79.55 | **7.7×** | 47.60 | 50.32 | 0.95× |

На NumPy 2.1.1 переход к пакетному gemm ускоряет операцию в 7.7–14.3 раза. На NumPy 2.5.2 разрыв исчезает: библиотека сама находит тот же путь, и оба способа дают одно и то же с точностью до разброса замера. Разложение на сечения, таким образом, опередило исправление в библиотеке, а не заменило его.

Существенно, что и после исправления путь сохранён в реализации: он не проигрывает ни на одной проверенной форме и делает время исполнения независимым от эвристик диспетчеризации, которые, как показывает та же таблица, меняются между выпусками. Совпадение результатов проверено перебором 49 сочетаний рангов и параметров λ, μ при расхождении на уровне машинного эпсилона.

Отметим границы применимости результата. На рассмотренном наборе OLAP-запросов ускорение не проявляется: проталкивание проекций (§3.2) заранее сворачивает частные оси операндов, и к моменту стягивания λ-произведения оказываются мелкими. Выигрыш относится к произведениям **куба на куб** — запросам по нескольким таблицам фактов с общим измерением, сохраняемым в группировке, — и к внутриузловому счёту при распараллеливании, где узел получает свёртку целиком (§4.9).

Итог раздела: способ обращения к BLAS следует выбирать по числу скоттовых индексов. При λ = 0 предпочтителен единый einsum, при λ ≥ 1 — разложение на сечения с пакетным gemm. Граница проходит ровно там, где свёрнутое произведение Соколова отличается от обычной тензорной свёртки.

Отдельно отметим, что применение внешней библиотеки оптимизации порядка стягивания [10] не изменило бы результата: используемая функция einsum_path [9] реализует те же алгоритмы динамического программирования и жадного выбора, а дополнительные стратегии такой библиотеки рассчитаны на тензорные сети из десятков операндов, тогда как планы рассматриваемого класса содержат три–пять.

### 4.7. Формат выдачи результата

При широкой выдаче время уходит не на свёртку, а на сборку результата, и потому исход сравнения зависит от того, в каком виде результат запрашивается. Замер на запросе, возвращающем 394 тыс. строк, приведён в табл. 14.

%ПОДПИСЬ% Таблица 14 — Влияние формата выдачи, 394 тыс. строк результата

| Формат | Настоящая работа | DuckDB |
|---|---|---|
| Кадр данных | 19.48 мс | 38.34 мс |
| Кортежи языка Python | 71.08 мс | 192.44 мс |

Различие велико и в обе стороны, поэтому сравнивать системы в разных форматах недопустимо: такой замер измеряет не движок, а способ упаковки ответа. В настоящей работе обе системы всюду запрашивались в одинаковом формате.

Приведённые числа получены при хранении результата **по столбцам** с ленивой сборкой строк; построение кадра из кортежей языка обходится на порядок дороже. Отсюда общее наблюдение: на запросах с широкой выдачей узким местом становится не алгебра, а представление результата.

### 4.8. Точность

Свёртка есть сумма произведений, поэтому точность определяется точностью суммирования. Измерение на сумме 10^{7} случайных слагаемых дало относительную погрешность 4.4·10^{−8} для одинарной точности и менее 10^{−15} для двойной. Погрешность 10^{−7} на агрегате порядка 10^{4} даёт абсолютное расхождение около 10^{−3}, что для денежных мер неприемлемо, поэтому по умолчанию используется двойная точность.

### 4.9. Проверка правила выбора оси дробления

Правило из §3.4 — резать по скоттовой оси, а не по свободной — выведено из утверждения работы [6] и потому нуждается в самостоятельной проверке. Измерялось разбиение свёртки A[l, s, c] ∗(1,1) B[s, c, m] между четырьмя узлами, где оси l и m свободны, а ось s скоттова. Мощности всех осей 200, кроме кэлиевой (120); каждый сомножитель занимает 36.6 МиБ. Для каждой оси разбиения фиксировался объём пересылок — суммарный размер полезных нагрузок, которые получат узлы, — и время их фактической подготовки: узел не может считать по чужому срезу, срез нужно выделить и скопировать. Результаты в табл. 15.

%ПОДПИСЬ% Таблица 15 — Цена разбиения по осям разного типа, четыре узла

| Ось | Роль | Переслано, МиБ | Рост | Реплицируется | Подготовка, мс | Счёт, мс |
|---|---|---|---|---|---|---|
| s | скоттова (λ) | 73.2 | 1.00× | ничего | 6.7 | 49.3 |
| l | свободная | 183.1 | 2.50× | B целиком | 0.0 | 29.7 |
| m | свободная | 183.1 | 2.50× | A целиком | 7.7 | 25.5 |

Результат во всех трёх случаях совпал с монолитной свёрткой, то есть выбор оси влияет на цену, а не на ответ, — как и должно быть по следствию 1.

По объёму пересылок правило подтверждается прямо: разбиение по скоттовой оси режет оба сомножителя, и суммарный объём равен объёму данных; разбиение по любой из свободных осей вынуждает реплицировать второй сомножитель на все узлы, и объём растёт в 2.5 раза при четырёх узлах — с ростом их числа отношение стремится к удвоению.

Объём пересылок здесь величина расчётная и от прогона к прогону не меняется; времена же — измеренные, и разброс между прогонами достигает четверти, поэтому содержательно в них лишь то, что все три величины одного порядка и что упорядочены они не так, как объёмы пересылок. Отсутствие выигрыша во времени счёта закономерно: разложение на сечения (§4.6) внесено в само ядро, поэтому узел получает его при любом разбиении. Тем самым выбор оси и разложение на сечения отвечают за разное — первое за объём пересылок, второе за внутриузловую арифметику, — хотя и следуют оба из одного утверждения работы [6] о строении результата при λ ≥ 1.

## 5. Обобщение на полукольца

Все доказательства предыдущих разделов используют лишь аксиомы коммутативного полукольца: ассоциативность и коммутативность обеих операций, дистрибутивность умножения над сложением, свойства нейтральных элементов. Существование обратного по сложению не используется нигде.

**Предложение 2.** Определение (λ, μ)-свёрнутого произведения корректно, а теоремы 1 и 2 со следствием справедливы над любым коммутативным полукольцом.

Это даёт формально корректное место агрегатам, невыразимым над парой сложения и умножения (табл. 16).

%ПОДПИСЬ% Таблица 16 — Свёрнутое произведение над различными полукольцами

| Полукольцо | Носитель | Смысл операции |
|---|---|---|
| (+, ·) | действительные числа | агрегат SUM; исходная алгебра Соколова |
| (∨, ∧) | {0, 1} | основа агрегата COUNT DISTINCT |
| (max, ·) | неотрицательные числа | агрегат MAX |
| (min, ·) | неотрицательные числа с плюс бесконечностью | агрегат MIN |
| (max, +) | действительные числа с минус бесконечностью | критический путь |
| (min, +) | действительные числа с плюс бесконечностью | кратчайшие пути |

Корректность обобщения подтверждается косвенно: повторное (min, +)-произведение матрицы смежности графа даёт матрицу кратчайших путей, то есть операция ведёт себя как полукольцевая, а не подогнана под нужный ответ.

### 5.1. Ограничение области определения

**Утверждение 3.** Пара (max, ·) не образует полукольца на всей числовой оси: дистрибутивность нарушается на отрицательных множителях, поскольку max(2, 3)·(−1) = −3, тогда как max(2·(−1), 3·(−1)) = −2.

**Следствие 2.** На знакопеременных данных теорема 2 для (max, ·) неверна: разные порядки свёрток дают разные ответы. Численный эксперимент на цепочке из трёх матриц с элементами из стандартного нормального распределения показал расхождение порядка 1.1 при данных единичного масштаба, тогда как на неотрицательных данных расхождение не превышало 4.4·10^{−16}.

Иными словами, на знакопеременных данных теряется корректность переписывания плана. Поэтому утверждение «MIN и MAX суть свёрнутое произведение над полукольцом» верно лишь с оговоркой о неотрицательности мер; в реализации соответствующая проверка выполняется перед вычислением. Тропические полукольца этого ограничения лишены, так как сдвиг сохраняет порядок.

### 5.2. Агрегат COUNT DISTINCT

Распространено мнение, что COUNT DISTINCT принципиально не выражается в матричной модели, поскольку требует мощности множества, а не суммы. Это верно лишь для числовых полуколец.

**Предложение 3.** Пусть A — гиперкуб факта, d — одна из его осей, G — набор осей группировки, не содержащий d. Тогда COUNT(DISTINCT d) с группировкой по G вычисляется композицией двух свёрнутых произведений: сначала (0, μ)-свёртка индикатора [A ≠ 0] над булевым полукольцом ({0,1}, ∨, ∧) по осям, не входящим ни в G, ни в {d}; затем (0,1)-свёртка результата с матрицей из единиц над (+, ·) по оси d.

*Доказательство.* Первая свёртка даёт величину, равную единице тогда и только тогда, когда для данной комбинации значений G и d существует хотя бы одна непустая ячейка, то есть индикатор присутствия значения. Вторая свёртка суммирует эти индикаторы по оси d, что и есть число различных присутствующих значений. □

Стоимость по памяти заслуживает отдельного замечания, поскольку буквальное следование доказательству дорого. Свёртка над (∨, ∧) не сводится к BLAS и в общем пути вычисляется широковещательным произведением с последующей редукцией, что материализует промежуточный массив размером с сам гиперкуб; вместе с приведением индикатора к числовому типу это давало **троекратный** расход памяти сверх куба (замер: 5.0 МиБ дополнительно при кубе 1.65 МиБ против 0.07 МиБ у обычного SUM). Между тем свёртка с матрицей единиц над (∨, ∧) поэлементно совпадает с прямой ∨-редукцией по тем же осям, а индикатор до самой редукции может оставаться булевым — по байту на ячейку вместо восьми. После обеих поправок дополнительный расход составил 1.87 МиБ, то есть порядка одного размера куба вместо трёх, при том же результате. Это частный случай общего замечания к §5, приведённого ниже: полукольцевая формулировка задаёт смысл операции, а исполняется она наиболее дешёвым эквивалентным способом.

Существенное ограничение: различаются значения измерения, то есть координаты по оси. Для DISTINCT по произвольной мере потребовалось бы вынести область её значений в отдельную ось, чего модель данных не предусматривает. Реализация поддерживает первый случай и отклоняет второй с объяснением. Результаты сверены с построчным вычислением на нескольких наборах данных.

Практическое замечание к §5 в целом: einsum и BLAS реализуют лишь пару сложения и умножения, поэтому прочие полукольца вычисляются широковещательным произведением с последующей редукцией, требующим материализации промежуточного массива. В исполнителе запросов агрегаты MIN и MAX по этой причине вычисляются прямой редукцией. Полукольцевая реализация служит обоснованием модели, а не рабочим путём.

## 6. Ограничения модели

Границы применимости очерчиваются столь же строго, как и возможности (табл. 17).

%ПОДПИСЬ% Таблица 17 — Что не выражается в модели

| Конструкция | Причина |
|---|---|
| MIN, MAX над (+, ·) | Доказано утверждением 2; выражаются над (max, ·) при неотрицательных мерах |
| COUNT DISTINCT по мере | Требует вынесения области значений меры в отдельную ось; по измерению — выражается (предложение 3) |
| ORDER BY, LIMIT, HAVING | Операции над результирующей таблицей, а не над гиперкубами; вне алгебры по природе |
| Объединение, разность, θ-соединения | Не реализованы; соединение возможно лишь по совпадению имён измерений |
| MIN, MAX и COUNT DISTINCT по соединению двух фактов | Отклоняются с объяснением: обе конструкции суть редукции над одним кубом, а не свёртки, и на произведение кубов не переносятся. SUM, COUNT и AVG по такому соединению поддержаны |
| Коррелированные подзапросы | Не реализованы; поддержан лишь простой подзапрос в условии принадлежности |
| Изменение данных (UPDATE, DELETE) | Не реализовано. Поддержана дозагрузка добавлением: словари измерений расширяются в конец, новые меры прибавляются к существующим ячейкам, версия куба увеличивается. Точечное изменение меры потребовало бы вычитания прежнего вклада из ячейки, что при ⊕-схлопывании слоя восстановимо лишь при выполнении условия предложения 1; удаление кортежа неразрешимо и в этом случае, если полукольцо не имеет обратного по сложению. Повторная загрузка того же факта перестраивает гиперкуб целиком |
| Изоморфизм моделей в общем случае | Имеет место лишь при условии предложения 1 |

К этому добавляется ограничение, общее для всей многомерно-матричной модели и теперь измеренное количественно (§4.4): время свёртки определяется числом ячеек гиперкуба, а не числом фактов, поэтому подход выгоден, лишь пока на ячейку приходится порядка одного факта и более; при 0.25 факта на ячейку он уступает колоночной СУБД и на простой агрегации, и на составном запросе. Поскольку число ячеек есть произведение мощностей измерений, требование к схеме получается жёстким: измерения должны быть достаточно грубыми. Переход к разреженному представлению снимает это ограничение лишь отчасти и лишь для простых свёрток (§4.5): на составных запросах разреженный путь уступает плотному при любой заполненности. Именно соотношение числа фактов и числа ячеек, а не разреженность сама по себе, и есть настоящая граница применимости подхода.

## Заключение

Построена и измерена программная реализация алгебраической машины баз данных на (λ, μ)-свёрнутом произведении многомерных матриц.

1. Установлено соответствие ролей индексов свёрнутого произведения конструкциям языка запросов: λ-индексы отвечают соединению с сохранением ключа группировки, μ-индексы — соединению с последующей агрегацией. Условие отбора, свёртка по иерархии, оконная функция и ограничение доступа выражаются добавлением операнда в ту же свёртку, а не отдельными проходами по данным.

2. Доказана теорема о разложении многооперандного стягивания в композицию бинарных свёрнутых произведений; из неё следует независимость результата от порядка свёрток и корректность оптимизации плана. Выигрыш от выбора порядка достигает сорока раз на типовом запросе.

3. Найдено точное условие обратимости отображения отношения в гиперкуб — набор измерений должен быть ключом отношения фактов. Отсюда следует, что соответствие реляционной и многомерно-матричной моделей в общем случае является гомоморфизмом, но не изоморфизмом, и объясняется различие гранулярности агрегатов MIN и MAX.

4. Доказана непредставимость MIN и MAX над полем действительных чисел и показано, что они, а также COUNT DISTINCT по измерению, выражаются над другими полукольцами — соответственно (max, ·) и ({0,1}, ∨, ∧). Для (max, ·) установлено ограничение области определения, без соблюдения которого теряется корректность переписывания плана.

5. Схема распараллеливания сопоставлена с алгоритмом [6]. Разбиение по скоттовым индексам, обоснованное там, не требует репликации сомножителей, тогда как разбиение по свободным осям реплицирует один из них; реализация следует этому правилу. Оно подтверждено измерением: при четырёх узлах разбиение по свободной оси увеличивает объём пересылок в 2.5 раза при неизменном результате.

6. Показано, что при λ ≥ 1 свёрнутое произведение сводится к пакетному матричному умножению разложением на сечения, обоснованным в [6]. Результат имеет и измеримую сторону, и методическую. Измеримая: на версии библиотеки, не выполнявшей такого сведения самостоятельно, разложение давало 7.7–14.3 раза; в последующей версии тот же путь найден самой библиотекой, и разрыв исчез. Методическая: величина этого эффекта оказалась привязана к версии библиотеки не слабее, чем к оборудованию, поэтому в §4.1 и §4.6 замеры поставлены как контролируемый опыт на двух версиях, а версии закреплены вместе с текстом.

7. Проведено сравнение с колоночной СУБД DuckDB и с ClickHouse на одиннадцати запросах с построчной сверкой результатов во всех трёх системах. Против DuckDB преимущество составляет от 1.3 до 4.5 раза, против ClickHouse — от 3.6 до 66; наименьшее оно там, где запрос сводится к одной агрегации, наибольшее — где несколько операций сливаются в одно стягивание. Симметричное сравнение, где все системы стартуют из предпосчитанной сводки одинаковой гранулярности, отделяет вклад алгебры от вклада предварительной агрегации. Развёртки по трём параметрам дали правило: время свёртки определяется числом ячеек гиперкуба, время колоночной СУБД — числом строк, поэтому отношение времён есть функция числа фактов на ячейку. Преимущество растёт с объёмом фактов — до 7.8 раза при 5 млн строк — и теряется при менее чем одном факте на ячейку. Однократное построение гиперкуба окупается начиная с 10–29 запросов на составных и с 458 на одиночной группировке.

8. Установлено, что выбор физического представления не сводится к заполненности: ниже 4 % заполнения разреженный путь выгоднее плотного на простой свёртке, но проигрывает ему на всех уровнях заполнения, когда операндов в плане четыре. Отсюда следует, что выбор представления должен приниматься оптимизатором по форме плана, а не загрузчиком по одной лишь заполненности.

Направления дальнейшей работы: реализация распределённого умножения на внутреннем уровне — обобщение алгоритма Кэннона по схеме [6], которого в настоящей работе нет; расширение класса представимых запросов за счёт полукольца множеств.

## Приложение. Минимальный пример трансляции

Пример показывает весь путь от запроса до элементарной операции ядра. Схема: гиперкуб продаж по измерениям (клиент, товар, дата) мощностей 100 × 100 × 120, справочник товаров с атрибутом «категория» и мерой «цена», иерархия дата → месяц.

%КОД% from amdb import Database
%КОД%
%КОД% db = Database()
%КОД% db.load_frame(sales, ["customer", "product", "date"], "quantity", "sales")
%КОД% db.load_dimension(products, "product",
%КОД%                   attributes=["category"], measures=["price"])
%КОД% db.add_hierarchy("date", "month", {i: f"M{i // 10}" for i in range(120)})
%КОД%
%КОД% print(db.explain(
%КОД%     "SELECT customer, month, SUM(sales.quantity * product.price) AS revenue "
%КОД%     "FROM sales JOIN product "
%КОД%     "WHERE product.category = 'A' "
%КОД%     "GROUP BY customer, month"))

Вывод плана:

%КОД% источники: sales, product
%КОД% GROUP BY: customer, month
%КОД% фильтр: Mask('product',) selected=50/100
%КОД% агрегат: SUM(quantity * price) AS revenue
%КОД%
%КОД% План выполнения:
%КОД%   GROUP BY: ['customer', 'month']
%КОД%   revenue: SUM
%КОД%       +1 x einsum('abc,b,cd,b->ad')  (0,2)-свёртка
%КОД%           [ sales['customer', 'product', 'date'],
%КОД%             price['product'],
%КОД%             rollup:date->month['date', 'month'],
%КОД%             mask:product['product'] ]
%КОД%   наличие групп: einsum('abc,cd,b->ad')  (0,2)-свёртка
%КОД%           [ sales__count['customer', 'product', 'date'],
%КОД%             rollup:date->month['date', 'month'],
%КОД%             mask:product['product'] ]
%КОД%
%КОД% Оценки:
%КОД%   revenue#0: ~1.34e+06 операций, макс. промежуточный 12 000 ячеек

В спецификации `abc,b,cd,b->ad` буквы отвечают измерениям: a — клиент, b — товар, c — дата, d — месяц. Она читается по правилам §2. Индексы b и c общие и в результате отсутствуют — это кэлиевы индексы, μ = 2: по товару свёртка выполняет соединение со справочником цен и последующую агрегацию, по дате — свёртку в месяцы. Индекс a свободен и сохраняется, индекс d вносится матрицей перехода иерархии. Четыре операнда — гиперкуб факта, вектор цен, матрица перехода дата → месяц и индикатор условия отбора по категории — стягиваются **одной** операцией: соединение, фильтр, свёртка по иерархии и агрегация не образуют последовательности проходов по данным, а являются сомножителями одного (0, 2)-свёрнутого произведения. Второй строкой плана идёт та же свёртка над спутниковым счётным гиперкубом: она отличает группу с нулевой суммой от отсутствующей группы (см. оговорку к предложению 1).

Запрос возвращает 1200 строк. Пример воспроизводится командой `python examples/appendix.py`; приведённый выше вывод плана получен этой командой.

## Литература

1. Соколов Н.П. Введение в теорию многомерных матриц. — Киев: Наукова думка, 1972. — 176 с.
2. Симаков В.А. Применение соглашения Эйнштейна для эффективной реализации (λ, μ)-свёрнутого произведения многомерных матриц. — 2026.
3. Емельченков Е.П., Левин Н.А., Мунерман В.И. Алгебраический подход к оптимизации разработки и эксплуатации систем управления базами данных // Системы и средства информатики. — 2009. — С. 114–137.
4. Мунерман В.И., Мунерман Д.В. Алгебраический подход к построению программно-аппаратных комплексов для повышения эффективности массовой обработки данных. — 2015.
5. Гончаров Е.И., Мунерман В.И., Синицын И.Н. Современные технологические средства создания многомерно-матричных машин баз данных // Системы высокой доступности. — 2024. — Т. 20, № 1. — С. 5–15.
6. Мунерман В.И., Мунерман Д.В. Обобщение одного алгоритма параллельного умножения матриц в алгебре многомерных матриц // Современные информационные технологии и ИТ-образование. — 2022. — Т. 18, № 3. — С. 566–577. DOI: 10.25559/SITITO.18.202203.566-577.
7. Морозов С.А., Мунерман В.И., Симаков В.А. Экспериментальный анализ многомерно-матричного подхода к построению маршрутов в графе // Известия вузов. Электроника. — 2022. — Т. 27, № 5. — С. 676–686. DOI: 10.24151/1561-5405-2022-27-5-676-686.
8. Муха В.С. Анализ многомерных данных. — Минск: Технопринт, 2004.
9. NumPy Developers. NumPy Documentation: numpy.einsum. URL: https://numpy.org/doc/stable/reference/generated/numpy.einsum.html (дата обращения: 27.08.2026).
10. Smith D.G.A., Gray J. opt_einsum — A Python package for optimizing contraction order for einsum-like expressions // Journal of Open Source Software. — 2018. — Vol. 3, № 26. — P. 753.
11. Raasveldt M., Mühleisen H. DuckDB: an Embeddable Analytical Database // Proceedings of the 2019 International Conference on Management of Data (SIGMOD). — 2019. — P. 1981–1984.
12. ClickHouse Documentation. URL: https://clickhouse.com/docs (дата обращения: 29.08.2026).
